# include <social.h>

struct individual *create_individual(struct individual *ind)
{
    ind = (struct individual *)malloc(sizeof(struct individual *)); 
    printf("Enter the ID\n");
}

int main()
{
    struct individual *ind =NULL;
    ind=create_individual(ind);
}